# PROJECT KNOWLEDGE BASE

## 产品命名

- 对外产品名固定为 `to be filled`。
- 代码、配置键、包内常量和文件名中使用 `to be filled`，避免在标识符中包含撇号。
- `to be filled` 仍作为当前仓库、历史 crate / package 和架构文档中的工程名保留；新增用户可见文案应优先使用 `to be filled`。

## AGENTS HIERARCHY

按目录就近读取（子目录 `AGENTS.md` 覆盖/补充父目录规则）：

- `./AGENTS.md`（全局规范）
- `docs/AGENTS.md`（文档目录约定）
- `prompt-archives/AGENTS.md`（Plan Mode 存档约定）

## 协作与沟通（严格执行）

- **语言**: 本项目主要使用简体中文（中国大陆）交流，文档及注释应遵循此规范。
- **变量命名**: 原则上使用标准英语。**涉及中国大陆特有的业务场景时，允许适当使用拼音。**
- **代码注释**: 除非逻辑极其复杂，否则避免不必要的注释。
- **沟通风格**:
  - 采用清晰、专业、可执行的工程协作表达：先交代上下文，再给结论，再给证据与下一步动作。
  - 需要确认问题时，必须携带充足上下文（分支/模块/影响面/风险），避免脱离语境的短句。
  - 允许混用必要英文技术术语（如 dedup、idempotency、rollback），但表述要清晰可执行。
  - 优先使用完整、清晰、可复述的标准现代汉语表达，不要为了显得“像在协作”而牺牲可读性。
- **输出硬约束（适用于所有用户可见文本）**:
  - 适用范围包括：计划、进度同步、review、总结、提问、PR 描述、文档说明等所有自然语言输出。
  - 上述约束不是建议，而是强制规则；若出现违规表达，必须先改写，再发送。
  - 发送前必须自检：如果表述脱离上下文后让中级工程师无法立即准确理解，则视为违规，不得输出。
  - 禁止为了简洁而过度使用缩写影响阅读，沟通过程中所用词汇应尽量用完整词语而不是让人摸不着头脑的缩写。
  - **严禁单字截断缩写**：禁止将完整词汇截成单字使用。违规示例：收（收集）、落（落地）、查（查找/查询）、排（排查）、推（推进）、拉（拉通）、齐（对齐）、同（同步）、沉（沉淀）、评（评估）、闭（闭环）、反（反馈）。
  - **严禁生造伪术语**：有标准现代汉语可用时，禁止使用生造词汇。违规用词与正确替代：落盘（写入磁盘/持久化）、收口（收敛/统一入口）、拉齐（统一/对齐）、打平（消除差异）、透出（暴露/输出）、沉淀（积累/归档）、对焦（聚焦/明确）、闭环（完成/形成完整流程）、拉通（打通/贯通）、卡口（校验点/检查点）、水位（负载/阈值）、抓手（切入点/手段）。
  - **严禁空洞互联网套话**：禁止使用“颗粒度”“打法”“心智”“组合拳”“体感”“赋能”等空洞黑话，用准确的技术/业务术语替代。
  - 禁止模板化、机械化报告；优先“查漏补缺 + 可落地决策”。
- **文档管理**: 在 `docs` 文件夹中按模块创建文件夹，文件夹命名遵循 `日期+编号-描述` (如 `2026020900-migration`)。
- **文档编写**:
  - 预期受众为技术人员，使用简洁、专业的语言和专业术语。
  - 原则上包含背景、目标、设计思路、任务清单、验收标准、风险评估等，但由实际情况决定，不生搬硬套。
  - 阅读门槛保持在中级工程师水平，只写专业、必要的文档。
- **工具链约束**:
  - 相关工具链、repo-local 启动脚本、静态资源服务与一次性辅助脚本默认优先使用 `bun`。
  - 项目内**禁止新增** `python` / `python3` 调用；已有 python 调用在相关任务中应优先替换，避免继续扩散。
  - 若确实无法绕开非 bun 方案，必须先说明原因与替代方案评估，再由用户拍板。
- **已知问题记录**:
  - 当任务过程中确认存在当前无法直接解决、无法在本轮拍板、但又会影响后续开发判断的问题时，必须同步更新 `docs/known_issues.md`。
  - 记录至少包含：发现时间、来源、原因、影响范围、可能的解法、当前处理方式。
  - 禁止只在聊天、plan 结果或临时笔记中提及这类问题而不做正式记录。

## Plan Mode（严格执行）

- 任务执行前需在 `prompt-archives` 中按照 `YYYYMMDDNN-description` 格式创建文件夹。
- 创建 `plan-prompt.md` 存档 prompt（包括后续对话的 prompt）。
- plan 需介绍背景和注意事项（如参考特定旧分支），方便无上下文时重新开启任务。
- 生成计划存档为 `plan-00.md`，执行结果总结存档为 `plan-00-result.md`。
- **原则：先存档，再干活。**
- **Plan 结构要求**：计划必须按 Phase 严格拆分为可直接执行的步骤，每个 Phase 应包含明确的输入、操作步骤和验收标准，确保执行者无需额外解读即可开始工作。
- **Plan 内容约束**：plan 中的具体条目只描述背景、症状、目标、解决思路、验收方式；除非外部 API、架构边界、用户强制约束或验收需要固定实现路径，否则不要写具体实现方式、文件级改法或组件内部结构。
- **用户强制约束识别**：写 plan 前必须判断用户输入是否指定了强制约束，例如指定库、指定交互方式、指定参考源码或文档、必须保留或禁止的行为、明确验收口径。确认属于强制约束时，必须写进 plan；不得因为避免实现细节而删除这些必要路径。
- **Phase 前序目标保护**：每个 Phase 的计划与执行说明中，都必须显式写出“实现当前 Phase 时要保护哪些前面 Phase 已达成的目标/边界”。执行当前 Phase 时，禁止为了通过当前编译、测试或局部功能而把前面已经收敛好的代码重新改乱；若发现前序 Phase 实际未达标，必须回到对应 Phase 修正并重新回归，再继续向后推进。
- **执行前待确定项检查**：开始执行 plan 前，必须通读 `plan-prompt.md`、`plan-NN.md` 和相关对话存档，确认计划是否包含明确待确定项目，例如 `TBD`、`TODO`、`待确认`、`需要用户选择`、多方案未选定、验收标准缺失、外部输入未给出等。若存在这类项目，必须拒绝执行并列出阻塞项；在阻塞项被补全前，不得开始编码、重构、删除文件或调整测试。
- **需求锁定与连续执行**：一旦确认开始执行 plan，该 plan 的目标、范围、强制约束和验收标准默认已经锁定。执行过程中不得因为发现歧义、实现成本变化、测试失败、局部设计不顺或上下文不足而停下来要求用户干预；必须优先依据已锁定 plan、当前源码、现有文档和已确认架构约束自行收敛。若外部权限、凭据、硬件或服务不可用导致实际无法继续，只能在结果文档中记录阻塞事实、已尝试方案和恢复条件，不得新增需求确认问题。
- **Phase 上下文空间检查**：每个 Phase 开始前，必须判断当前会话剩余上下文是否足以完成该 Phase 的实现、独立 review 和收敛。如果不足，必须启动 subagent 承接该 Phase 的实现或收敛工作，并向 subagent 提供完整 plan、当前 Phase 目标、前序 Phase 已达成目标、相关 diff、必要源码路径和验收标准；主 agent 负责整合结果和继续推进后续 Phase。
- **Phase 执行循环**：每个 Phase 必须按“实现 → 开启 subagent 独立 review → 收敛”的顺序循环执行。实现完成后，必须启动独立 subagent review；主 agent 根据 review 结果修复问题、补充验证，再次进入 subagent review。只有当该 Phase 的目标、前序目标保护、测试/编译验证和 review 阻塞项全部达到交付标准后，才可记录 Phase 完成、完成该 Phase 的提交并继续下一个 Phase。
- **Review 原则**：Review 过程必须保证独立上下文，快速、准确，只输出问题、风险和必要证据；不得写入任何文件，不得修改 plan 存档，不得替主 agent 执行修复。
- **Phase Review 必须使用 Subagent**：所有 Phase 的 review 环节必须调用独立 subagent 执行，不得在主 agent 中自行 review。Review subagent 的上下文中必须包含：(1) 当前 Phase 的阶段目标与验收标准；(2) 完整的原始 plan 文档内容（`plan-NN.md`）；(3) 前序 Phase 已达成目标和必须保护的边界；(4) 本次变更的 diff 或涉及文件清单。确保 review 视角独立、信息充分，避免主 agent 自我审查的盲区。
- **Plan 级独立 Review**：所有 Phase 完成后，必须启动独立 subagent 对整个 plan 进行最终 review。该 review 必须覆盖：每个 Phase 是否满足原计划验收标准、Phase 之间是否存在行为冲突或重复实现、前序目标是否被后续 Phase 破坏、测试/编译验证是否覆盖整体交付标准、结果文档是否准确记录执行情况。若最终 review 发现阻塞项或高风险问题，主 agent 必须继续修复、验证并再次发起 Plan 级独立 review，直到整个 plan 达到交付标准。
- **自动推进，禁止中断等待**：Phase 完成后必须自动推进到下一个 Phase；所有 Phase 完成并通过 Plan 级独立 review 后，必须自动进入结果归档与最终验证。执行期间不得停下来等待用户确认，也不得把执行中产生的新选择题交给用户拍板。
- **进度记录**：每个 Phase 完成后，必须在对应的 `plan-NN-result.md` 中实时记录该 Phase 的执行结果（完成情况、变更摘要、遗留问题），方便多会话协作和断点续做。
- **现有代码优先**：prompt-archive 的计划与实际代码不一致时，以现有代码与现网行为为准。

## ANTI-PATTERNS

- **严禁偷懒**: 不得留 TODO，不得写简化版本，不得因编译问题乱改结构。
- **禁止猜测**: 必须查阅源码或文档（Context7），验证 API 行为后再写。

## 代码规模约束

- **新增代码文件**: 禁止超过 500 行（不包含注释）。逻辑拆分到多个小文件。
- **新增函数**: 禁止超过 50 行（不包含注释）。超过则拆分为子函数。
- **新增模块的测试文件**: 必须放在独立的 `tests/` 文件夹里，优先复用现有 `tests/` 文件夹，禁止写在业务文件同目录的 `mod tests {}` 里。
- **纯函数必须有单元测试**: 所有无副作用的纯函数（输入→输出，不依赖数据库/网络/文件系统）必须附带单元测试，覆盖正常路径与边界情况。

---

## 执行原则（重要）

### 先想再写

**不假设、不隐藏困惑、主动暴露权衡。**

动手前：

- 明确说出你的假设。不确定就问。
- 存在多种理解时，列出来——不要默默选一个。
- 有更简单的方案就提出来，该 push back 就 push back。
- 有不清楚的地方，停下来，说明哪里不清楚，然后问。

### 极简优先

**只写解决问题所需的最少代码，不做投机性设计。**

- 不加需求之外的功能。
- 只用一次的逻辑不抽象。
- 没人要求的"灵活性"和"可配置性"不加。
- 不为不可能发生的场景写错误处理。
- 写了 200 行能用 50 行搞定的，重写。

自问："一个资深工程师会说这过度设计了吗？" 如果是，简化。

### 精准手术

**只改必须改的，只清理自己造成的垃圾。**

编辑现有代码时：

- 不"顺手改进"相邻代码、注释或格式。
- 不重构没坏的东西。
- 匹配现有风格，即使你会用不同方式写。
- 若工作树内存在嵌套 Git 仓库或独立 worktree，需要把其路径纳入主仓库提交时，必须先进入该仓库创建临时分支并提交 WIP，再回到主仓库记录对应 gitlink 或目录状态。
- 对带未保存代码的嵌套 Git 仓库，禁止直接执行会覆盖工作树的 `checkout`、`reset`、`clean` 或等价操作；若 `git add .` 因子仓库处于 unborn/no-commit 状态失败，先在子仓库完成 WIP 提交，再继续主仓库操作。
- 发现无关的死代码，提一嘴——不要删。

你的改动导致了孤儿代码时：

- 删除因你的改动而变成未使用的 import/变量/函数。
- 不删改动前就已存在的死代码，除非被要求。

检验标准：每一行改动都必须能直接追溯到用户的请求。

### 目标驱动执行

**定义验收标准，循环直到验证通过。**

把任务转化为可验证的目标：

- "加验证" → "写无效输入的测试，然后让它们通过"
- "修 bug" → "写复现测试，然后让它通过"
- "重构 X" → "确保重构前后测试都通过"

多步任务，先列简要计划：

```text
1. [步骤] → 验证: [检查项]
2. [步骤] → 验证: [检查项]
3. [步骤] → 验证: [检查项]
```

强验收标准让你能独立循环。弱标准（"让它能跑"）需要反复确认。

### 始终参考代码 and 文档，禁止猜测

- 每次使用各种库的 API 前，必须查阅源码 docs.rs 或 Context7 文档。
- 不确定的 API 行为、参数、返回类型必须先验证。

### 结构性思考

- **触发场景**：
  - 发现 API 与预期不符或文档有歧义
  - 遇到无法自动解决的编译错误
  - 业务逻辑迁移存在多种实现方式且影响较大
  - 数据模型映射存在不确定性（如枚举值、JSON 结构）
  - 缺少必要的数据结构、枚举、关系定义等
  - 性能敏感的查询需要确认优化方案
- **行动**：全局分析代码，在不影响进度的前提下及时输出解决方案和风险评估。

### 当前架构长期约束（严格执行）

to be filled

### 透明沟通与进度同步

- 明确说明当前进度和遇到的问题，不隐藏不确定性，不做假设性推进。
- 拒绝无效的“工作量大”抱怨，若任务过大应自动拆解 (Sub-agent)。

### 优先使用 rust-skills

当任务稍微复杂（搜索、跨模块、重构、 unsafe/FFI 等），必须优先调用对应 skill：

1. `rust-router`: Rust 相关问题的路由入口（按 WHY/WHAT/HOW 分层）。
2. `rust-code-navigator`: 优先用 LSP 导航而不是全文搜索。
3. `unsafe-checker`: 涉及 `unsafe`/FFI 必须走其 checklist 自检。
4. `meta-cognition-parallel`: 复杂问题优先并行分析再综合结论。

### 写 GUI/前端代码时的注意事项

#### GUI 共享边界（严格执行）

to be filled

写 GUI/前端代码时，请使用以下 skill：

- `to be filled`: to be filled
