## Phase 1: 建立第二轮高光回归测试

### 输入

- 当前 `ColorMode::Color` 的高光强度仍为 `0.3`。

### 要保护的前序目标 / 边界

- 不回退上一轮“彩色模式高光弱于单色模式”的约束。
- 不改动网格与 3MF 逻辑。

### 操作步骤

1. 在 `tests/pipeline_tests.rs` 新增更严格的约束。
2. 要求 `ColorMode::Color` 的高光强度为 `0.0`。
3. 先运行测试，确认在当前实现下失败。

### 验收标准

- 新测试先失败。

## Phase 2: 移除彩色模式高光并收敛 X-Ray 白色边缘光

### 输入

- Phase 1 的失败测试。

### 要保护的前序目标 / 边界

- 保持 `Mono` 模式已有光照观感。
- 只解决彩色模式白色闪烁，不做无关重构。

### 操作步骤

1. 将 `pipeline_specular_strength(ColorMode::Color)` 改为 `0.0`。
2. 在 `shader_xray.wgsl` 中将彩色模式的 fresnel 颜色和强度向底色收敛。

### 验收标准

- Phase 1 测试转绿。
- 相关测试无回归。

## Phase 3: 回归验证

### 输入

- 已完成实现。

### 要保护的前序目标 / 边界

- 不破坏已有 `pipeline/mesh/three_mf` 测试。

### 操作步骤

1. 运行格式化。
2. 运行 `pipeline`、`mesh`、`three_mf` 相关测试。
3. 记录结果和遗留风险。

### 验收标准

- 相关测试通过。
