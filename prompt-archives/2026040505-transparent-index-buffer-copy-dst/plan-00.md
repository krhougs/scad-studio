## Phase 1: 建立 usage 约束测试

### 输入

- 当前透明索引缓冲会被 `Queue::write_buffer` 更新。

### 要保护的前序目标 / 边界

- 不改动透明排序算法本身。

### 操作步骤

1. 为透明索引缓冲 usage 提取纯函数。
2. 新增测试，要求该 usage 同时包含 `INDEX` 与 `COPY_DST`。
3. 先运行测试，确认在现状下失败。

### 验收标准

- 新测试先失败。

## Phase 2: 修复透明索引缓冲 usage

### 输入

- Phase 1 的失败测试。

### 要保护的前序目标 / 边界

- 只修复缓冲 usage，不顺手改透明 pass 的其他逻辑。

### 操作步骤

1. 在 `src/renderer.rs` 中让透明索引缓冲使用包含 `COPY_DST` 的 usage。
2. 继续复用现有索引缓冲创建逻辑，保持改动最小。

### 验收标准

- Phase 1 测试转绿。
- 相关测试通过。

## Phase 3: 回归验证

### 输入

- 已完成实现。

### 要保护的前序目标 / 边界

- 不破坏 `mesh`、`pipeline`、`three_mf` 测试。

### 操作步骤

1. 运行格式化。
2. 运行相关测试。
3. 记录结果。

### 验收标准

- 测试通过。
