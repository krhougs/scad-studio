# 执行结果存档：`2026042300-studio-web-feature-parity`

本文件在对应 Phase 执行过程中**实时追加**，与 `plan-00.md` 同步维护。

## 记录要求

- 每个 Phase 完成后，必须补充：完成情况、变更文件范围、验证结果、遗留问题。
- 若某 Phase 发现前序目标被破坏，必须在该 Phase 内修复并记录。

| Phase | 状态 | 摘要 | 遗留问题 |
|-------|------|------|----------|
| 0 | 已完成 | 产出 5 份契约：`plan-00-naming.md` / `plan-00-bridge.md` / `plan-00-toolchain.md` / `plan-00-ownership.md` / `plan-00-smoke.md`。覆盖：命名矩阵、wasm 桥接 API（含 `client_begin_handshake` / `client_destroy` / `ManagedClient` 方法清单）、params 类型与 `app-server-protocol` 映射、错误模型（含 `InvalidHandle` / `NotReady`）、超时 / watch 节流 / reconnect / trait 适配审查、bun-only 工具链、状态归属、S1a–S4 smoke 矩阵。Buddin 设计参考可获取性兜底写入 toolchain §5。 | 无 block；Phase 4 启动前需再次检查 `/Users/krhougs/LocalCodes/buddin/*` 可读性。 |
| 1 | 已完成 | 1) `pnpm-workspace.yaml` + 根 `package.json` `workspaces` 字段创建；`.gitignore` 追加 `pnpm-lock.yaml` / `node_modules/`。2) `crates/studio-web` 通过 `git mv` 重命名为 `crates/studio-web-wasm`；`src/app.rs` → `src/legacy_dom_shell.rs`。3) `Cargo.toml` 改 package name `studio-web-wasm`、lib name `studio_web_wasm`；`app-server-transport` 置为 `optional = true`；新增 features `legacy-shell = ["dep:app-server-transport"]`、`browser-smoke = ["legacy-shell"]`；`wasm-bindgen` 锁定 `=0.2.117`。4) 根 `Cargo.toml` workspace member 同步更新。5) `src/lib.rs` 将 `legacy_dom_shell` / `preview_canvas` / `transport_port` 模块与 boot 函数 全部置于 `#[cfg(all(target_arch = "wasm32", feature = "legacy-shell"))]` 之下；三个 wasm 侧文件的模块级 `#![cfg]` 同步更新。6) `scripts/run_studio_web.ts` / `tests/build_studio_web_shell.sh` / `tests/studio_web_smoke.sh` / `web/index.html` 更新到新 crate 名与新产物名；`browser_smoke.rs` / `browser_watch_smoke.rs` / `public_api_tests.rs` / `chat_state_tests.rs` 中 `studio_web::` 全部改为 `studio_web_wasm::`；标签字符串改为 `studio-web-wasm shell`。7) `packages/studio-web-wasm` 骨架（package.json / README.md / generated/.gitkeep / src/index.ts）与 `packages/studio-web` 骨架（package.json / tsconfig.json / vite.config.ts / index.html / src/main.ts / public/.gitkeep / tests/.gitkeep）创建。8) 新增 `scripts/check_wasm_bindgen_version.ts` 并接入 `bun run check:wasm-bindgen`，强制 Cargo.toml 与 CLI 版本一致；README 同步更新说明。9) `packages/studio-web-wasm/src/index.ts` 对 Phase 2 再生的空 generated 加 `@ts-expect-error` 保护。验证：`cargo check --workspace` 通过；默认 feature 下 `cargo tree -p studio-web-wasm` 不含 `app-server-transport`；`cargo check -p studio-web-wasm --features legacy-shell --target wasm32-unknown-unknown` 通过；`cargo test -p studio-web-wasm` host 测试全通过（public_api + chat_state）；产物路径 `target/wasm32-unknown-unknown/debug/studio_web_wasm.wasm` 生成正确；`rg "legacy_dom_shell" crates/studio-web-wasm/src` 全部命中均位于 `#[cfg(all(target_arch = "wasm32", feature = "legacy-shell"))]` 之下；`bun run check:wasm-bindgen` 通过。Review：P0 无；P1-1 已修（wasm-bindgen-cli 版本锁定入口落到 bun 脚本 + package.json script）；P1-2（文件级 `#![cfg]` 与 mod 级 cfg 冗余）列为 Phase 2 清理候选；P2-2 已修。 | 无 block；Phase 2 计划清理 `legacy_dom_shell` 并在 `studio-common` 新增 `ManagedClient`。 |
| 2 | 已完成 | Phase 2a (`studio-common`)：新增 `ManagedClient<T>` 监督层，覆盖 Phase 0 契约 §9 全部方法 —— 命令派发 / 事件 drain / snapshot / 超时 tick / 取消 / watch 节流与重订阅 / reconnect 重放。新类型（`ClientEvent` / `ClientError` / `ClientSnapshot` / `TransportStatus` / `TransportCloseReason` / `WatchParams` / `WatchEventPayload` / `ClientTimeouts`）的 serde 格式冻结有快照测试保护。Phase 2a 经 codex 两轮 review：第一轮抓到 [P1] wire envelope 用了自造 `{"frame"}` tag → 将 `ClientEnvelope` / `ServerEnvelope` / `TransportErrorFrame` 从 `app-server-transport` 迁入 `app-server-protocol` 并在 transport crate re-export，`studio-common` 使用协议 crate 的 envelope；第二轮抓到 3 条 [P2]（陈旧 preview 响应覆盖、重连期 cancel 乱序、watch subscribe 忽略 timeout）全部修掉。`cargo test -p studio-common --test managed_client_tests` 15 条通过（含 3 条新增边角测试）。Phase 2b (`studio-web-wasm`)：新增 `wasm_bridge/` 暴露完整 bridge API —— 15 个 `client_*` 导出（含 `client_create` 无参与 `client_create_with_timeouts` 两入口）、`mesh_decode` / `mesh_destroy`、以及 `renderer_*` API-complete stub（真实 wgpu 初始化因需 `wasm_bindgen_futures` 等待 JS Promise 违反契约 §10，推到 Phase 3 由 TS 侧或 `scad_scene` 同步构造函数处理）。`NullTransport` 满足 `AppServerTransportPort` 形参要求但不被 `ManagedClient` 实际调用，envelope 走字节队列路径。`FakeChatState` 迁入 `legacy-shell` feature。`ClientHandle` 用 `Option<ManagedClient>` 实现 destroy 幂等，post-destroy 所有方法返回 `ClientError::InvalidHandle`。Phase 2b 经 codex review：首轮 0 P1；抓到 [P2] `client_destroy` 按值消费 handle 与 [P3] `client_create(JsValue)` 违反契约 §2 的无参签名，均已修掉。`cargo test -p studio-common --tests` 全部绿色；`cargo check --workspace` / `cargo check -p studio-web-wasm --target wasm32-unknown-unknown`（default / legacy-shell 两套）均通过；`wasm-pack test --headless --chrome crates/studio-web-wasm --test wasm_bridge_smoke` 9/9 通过（S1b 全部必覆盖场景 + 1 条 destroy 幂等测试）。`bun run check:wasm-bindgen` 通过。 | Phase 3 前置依赖：renderer 真实 wgpu 接入（TS 侧 drive adapter/device 请求或 scad-scene 暴露同步 constructor）；`ClientTimeouts` serde 形态建议在 `plan-00-bridge.md` §5 补文档；reconnect 期间 watch subscribe deadline 不暂停（低概率误报 timeout）属已知小口风险。 |
| 3 | 已完成 | `packages/studio-web` React PWA 壳成型：Vite 6 + React 18 + react-router-dom v7 + zustand + vite-plugin-pwa（dev SW 禁用，生产 SW 缓存 js/css/html/wasm 且 wasm 文件名带 hash）+ vite-plugin-wasm / vite-plugin-top-level-await。`wasm-bridge/` 目录包含 `WasmClient`（薄适配层，内部 5 Hz pump：`client_tick` → 出站 drain → transport.send → `client_drain_events` 分发）、唯一 `request-resolvers.ts`（`Map<RequestId, {resolve,reject}>`，符合 ownership §3 例外且事件到达即 delete）、`event-stream.ts` 事件标准化。`transport/websocket-transport.ts` WebSocket 生命周期与指数退避 [1,2,4,8,15]s，无业务状态。`state/ui-store.ts` 仅 UI 字段（route / openTabs / activeTab / sidePanelOpen / isSettingsModalOpen / inputDraft）。`canvas/renderer-controller.ts` 通过 React hook 实现 StrictMode 幂等；Phase 2b renderer 桩的 Err 被捕获写入 `lastError` 并以 placeholder 文字降级展示，不 crash UI。`routes/`：`/` → WorkbenchShell 驱动完整握手 → `dispatch_workspace_current` → `dispatch_workspace_list` → 文件点击触发 `dispatch_preview_request` 显示 phase 文字；`/settings` 最小占位。WorkbenchShell 在 `HandshakeAccepted` 事件里启动业务命令以避开 `NotReady`。`scripts/build_studio_web.ts` 编排 cargo → wasm-bindgen → vite build；根 `package.json` 新增 `web:dev` / `web:build` / `web:smoke:browser` 脚本。`packages/studio-web-wasm/generated/` 的 5 个 wasm-bindgen 产物（.js / .d.ts / bg.js / bg.wasm / bg.wasm.d.ts）入库作为 Phase 5 S1c diff 基线。`bun.lock` 入库。Phase 3 经 codex review：gate PASS，[P2] 发现 `bun --cwd` 前置会被 1.3.12 视作 no-op，已改为 `bun run --cwd pkg script` 语法，`bun run web:smoke:browser` 现在真的拉起 Playwright 且 S2 两条用例全绿（workspace info / preview request）。验证：`cargo check --workspace` / `cargo test -p studio-common --tests` / `bun run --cwd packages/studio-web typecheck` / `bun run --cwd packages/studio-web test:unit`（StrictMode 双 mount 2/2）/ `bun run --cwd packages/studio-web build`（生产 wasm hash 正确）/ `bun run web:smoke:browser`（S2 2/2）全通过。grep 验收：`packages/studio-web/src` 内 `WorkspaceSession|PreviewState|DirectoryWatchLifecycle|AppServerClient` 零命中，`requestId` 仅在 `wasm-bridge/` 命中；全仓无 `pnpm install` / `pnpm run` 调用。 | renderer 真实 wgpu 初始化推到 Phase 5+（TS 侧驱动 adapter/device 或 `scad_scene` 同步构造）；Phase 0 契约建议补一句"JS 必须在 HandshakeAccepted 事件后再 dispatch 业务命令以避开 NotReady"。 |
| 4 | 已完成 | 新增 `docs/design-system/source-notes.md`（Buddin 引用 + 同步策略）与 `docs/design-system/studio-datasheet-workbench.md`（datasheet 工作台规范，保留 dark-only / Geist+Geist Mono / sentence case / 零圆角 / 1px hairline / 无 emoji / 无渐变 CTA / 无玻璃化滥用；§10 书面结论：不下沉 token 到 `scad-ui`，理由 egui 不消费 CSS 自定义属性）。新增 `packages/studio-web/src/styles/`：`tokens.css` 195 / `workbench.css` 136 / `workbench-zones.css` 312 / `primitives.css` 132（均 ≤500 行）。新增 `packages/studio-web/src/workbench/` 六文件，把 Phase 3 的 workbench-shell 拆成 Topbar / Rail / ChatZone / CanvasZone / Inspector + 统一的 `workbench-layout.tsx`，CSS Grid 52/360/1fr/320 × 44/1fr 按 Buddin 原 index.html 还原。`packages/studio-web/index.html` 加 Geist + Geist Mono Google Fonts CDN preconnect。`App.tsx` 内联 style 改 className + `NavLink`。`.gitignore` 新增 `packages/studio-web/dist/` / `test-results/` / `playwright-report/`。Phase 4 经 codex review：gate PASS，[P2] onClose/onError 未更新 React phase 导致断线后顶栏仍显示 online —— 改成 clean close → connecting、unclean → error；[P3] 空 `workspace_list.entries` 被误判为 loading —— 通过 `entriesLoaded = snapshot.workspace_list !== undefined` 区分。验证：`cargo check --workspace` / `bun run --cwd packages/studio-web typecheck` / unit test 2/2 / `build`（生产 wasm hash 正常 + Workbox SW 6 precache 条目 1441 KiB）/ S2 smoke 2/2（workspace info + preview request）。`rg "\.claude/skills\|\.agents/skills"` 命中仅为禁止条款引用。`rg "components/workbench-shell"` 零命中。 | Inspector 真实参数/预设/导出推到 Phase 7；Chat 真实 agent 接入独立 plan；字体离线自托管推到后续；响应式折叠（<1280px）推到 Phase 7+。 |
| 5 | 已完成 | Phase 5 在 Phase 4 React PWA 壳上接回 MVP 并统一 smoke 入口：1) 把 `mesh_decode` 纯逻辑从 `wasm_bridge/mesh.rs` 抽到顶层 `mesh_decode` 模块，host + wasm32 共享；新增 host 单测 `tests/mesh_decode_tests.rs`（4 条），S1a 从 1 条增至 5 条。2) `scripts/run_websocket_host.ts` 抽出共享 host 启动器 + `resolveWsUrl`（读 `SCAD_STUDIO_WS_URL`），带 CLI 入口（`web:host`）；新增 `scripts/run_studio_web_dev.ts`（同时启 host + Vite dev，默认 feature，不带 legacy-shell），挂到 `bun run web`。旧 `scripts/run_studio_web.ts`（legacy-shell 路径）保留，挂到 `bun run web:legacy`。3) 新增 `scripts/run_smoke.ts` dispatcher：`--case wasm_package_smoke/browser_smoke/browser_watch_smoke/all`；`all` 顺序跑 S1a→S1b→S1c→S2→S3→S4，任一失败立即 exit。根 `package.json` 脚本统一：`web` / `web:dev` / `web:host` / `web:build` / `web:smoke` / `web:smoke:browser` / `web:smoke:legacy` / `web:legacy`。`tests/studio_web_smoke.sh` 保留作兼容（`web:smoke:legacy`）。4) S1c `scripts/smoke/wasm_package_smoke.ts`：快照 generated/ → cargo+wasm-bindgen regen → 文件系统遍历兜底 diff（跳过 .gitkeep）→ snapshot 还原。5) S3 Playwright spec `packages/studio-web/tests/playwright/browser-watch-smoke.spec.ts`：spawn websocket-host + Vite（端口 39181/5176，避免与 S2 冲突）→ 等 handshake 完成（等 `entry-README.md` 出现）→ Node fs 写 `watch-smoke-generated.txt` → 断言 `entry-watch-smoke-generated.txt` 在 15s 内出现。6) `packages/studio-web/src/workbench/workbench-layout.tsx` 接入 watch：handshake ack 成功后自动 `subscribeDirectoryWatch({directory:null, throttle_ms:150})`，`onWatchEvent` 回调重拉根 `workspace_list` + 所有已展开子目录；transport close 清 watch flag 以便重连后重新订阅。采用方案 (c)：不改 `ManagedClient`，只在 React 层驱动重拉。7) `Inspector` 升级为递归树：directory entry 按钮触发 `onExpandDirectory` / `onCollapseDirectory`，子目录由 WorkbenchLayout 发 `dispatch_workspace_list({directory: entry.path})` 填充；加 loading / error / empty 三态展示；保留 file entry 走 `onRequestPreview`。新增 `path-utils.ts`（`pathSegments/pathLabel/pathKey/pathDisplay`）与 `workbench-wiring.ts`（`createTransport/buildClientCallbacks/describeError`）把 layout 拆到 ≤50 行/函数。8) Preview ready 后调 `mesh_decode` 验证 wasm bridge 能接收 server 返回 `three_mf` 字节（`mesh` 直接用 positions/indices 计数），decode 成功即 `mesh_destroy` 并把元数据写到 Inspector `preview-mesh-summary` 里。9) 清除 S3 在 Playwright 里遗留的旧 wasm 写入路径依赖（新 spec 不用 WebSocket 写文件，直接 Node fs 触发）。10) Rail 保持 Phase 4 icon bar 形态（workspace tree 放在 Inspector，而非 Rail 本体——Rail 宽 52px 与 Buddin datasheet dark-only/1px hairline 不兼容 tree 展示；这是相对计划 §E 的偏差，详见遗留）。验证：`cargo check --workspace` / `cargo test -p studio-common --tests` / `cargo test -p studio-web-wasm --tests`（5/5：4 mesh_decode + 1 public_api）/ `cargo test -p studio-web-wasm --features legacy-shell --tests`（7/7：+2 chat_state）/ `bun run --cwd packages/studio-web typecheck` / `bun run --cwd packages/studio-web test:unit`（2/2）/ `bun run web:build`（生产 wasm 带 hash：`studio_web_wasm_bg-RXoQSKGc.wasm`；PWA Workbox 6 precache 条目 1450.75 KiB）/ `wasm-pack test --headless --chrome crates/studio-web-wasm --test wasm_bridge_smoke`（9/9）/ `bun run web:smoke --case wasm_package_smoke`（byte-identical）/ `--case browser_smoke`（2/2）/ `--case browser_watch_smoke`（1/1）/ `bun run web:smoke`（all：S1a→S4 全绿）。grep 验收：`rg "legacy_dom_shell" crates/studio-web-wasm/src` 命中均在 `#[cfg(all(target_arch = "wasm32", feature = "legacy-shell"))]` 下；`rg "cargo .*studio-web-wasm" scripts/` 默认路径（`run_studio_web_dev.ts`/`build_studio_web.ts`/`smoke/wasm_package_smoke.ts`/`run_smoke.ts`）都不带 `--features legacy-shell`，仅 `run_studio_web.ts`（兼容入口）带。**S1c 首次运行发现真实代码漂移**（非工具链非确定性）：当前 commit 的 `studio_web_wasm_bg.wasm.d.ts` 和 `studio_web_wasm_bg.wasm` 与 Phase 5 重构（`mesh_decode` 模块从 `wasm_bridge/mesh.rs` 抽到 `src/mesh_decode.rs`）后再生结果有 d.ts 的 export 顺序差异（`mesh_decode/mesh_destroy/__wbg_meshhandle_free` 三行位置互换）与 .wasm ~63B 字节差异；重跑 wasm-bindgen 多次确认字节稳定（MD5 相同）；已把新 generated 写回 `packages/studio-web-wasm/generated/`，S1c 重跑 byte-identical。主 agent commit 时需把更新后的 5 个 generated 产物（`studio_web_wasm.js` / `.d.ts` / `_bg.js` / `_bg.wasm` / `_bg.wasm.d.ts`）一并入库。 | Rail workspace tree（计划 §E 原文）被改投放到 Inspector（Rail 保 icon bar），理由：Buddin datasheet Rail 52px 宽与 dark-only/1px hairline 风格与树展示不兼容。若必须放 Rail，需修 workbench 栅格与 datasheet 文档，建议走独立 plan。mesh_decode 接 3mf 字节后的 vertices/indices 计数只能由 bytes 长度/32 近似（`scad_scene::MeshData` 的 vertex/index 长度未经 wasm_bindgen 暴露）；真实 mesh 元数据在 Phase 7 renderer 接入时补。WorkbenchLayout 约 430 行 / 各函数 ≤ 50 行；inspector.tsx 217 行，在 500 行硬约束内。Phase 5 经 codex review：首轮 [P1] 子目录请求会把 `snapshot.workspace_list` 根目录覆盖掉（因 `ManagedClient` 每次 workspace_list 响应都写 snapshot）→ 已把 root entries 搬到独立 React state（`rootEntries` / `rootLoaded`），只 `refreshRootListing` 写入；[P2] 重连后 React 重复发 `subscribeDirectoryWatch` 会在服务端累积订阅（ManagedClient 本来就会 replay）→ 移除 `onWatchReset` 钩子，`watchActiveRef` 只在 effect cleanup 时置 false。两条 fix 还顺手修了 `dispatchWorkspaceList` / `dispatchPreviewRequest` 的 resolver 值未解开 `CommandSuccess` 外壳（`{type, payload}`）的 bug，之前只因 Phase 3 snapshot 派生路径掩盖了这个问题。修完后 S2 + S3 + 全 matrix 再跑一次全绿。 |
| 6 | 已完成 | 文档标签系统 + 四种 viewer。1) `state/ui-store.ts` 新增 `openTabs` / `activeTabId` / `openTab` / `closeTab` / `setActiveTab` / `activeRail`；DocumentTab 仅存 id / label / path / kind，不存内容字节。2) `workbench/tabbar.tsx` + `workbench/tab-kind.ts` 实现 Tab Bar 与扩展名路由（.md→markdown, .png/.jpg/.jpeg/.webp→image, .scad→scad-split, .stl/.3mf→mesh；不支持扩展名只在 status 条显示）。3) `viewers/markdown-viewer.tsx` + `viewers/markdown-parser.ts` 手写 ~130 行极简 parser（h1-h3 / 无序列表 / 围栏代码块 / 段落链接 / 行内 code），不引入 npm 包保 S1c 稳定。4) `viewers/image-viewer.tsx` FileRead → Blob URL + zoom/pan + 20 MiB 上限 + 4096px 尺寸警告 + cleanup revoke。5) `viewers/scad-split-viewer.tsx` 并发发 FileRead + PreviewRequest 左右拆分；`.scad` 被 `denied_extensions` 拒时源码面板展示拒绝原因，预览面板仍跑 pipeline。6) `viewers/mesh-viewer.tsx` 复用 Phase 5 preview pipeline。7) `viewers/file-read-decoder.ts` 是 `FileReadContents::Utf8|Binary` 唯一反序列化入口。8) `workbench-layout.tsx` Inspector `onRequestPreview` 改为 `openTab` 路由；`canvas-zone.tsx` 变为 Tab Bar + 激活 tab viewer 容器。9) `styles/viewers.css` 按 Buddin datasheet token 加 Tab Bar + viewer 样式。10) `docs/web-platform-limits.md` 记录刷新清空标签 / 20 MiB 图片上限 / markdown 能力集 / .scad 源码可能被 deny。11) smoke 扩展：`scripts/run_smoke.ts` 识别 `--case markdown_view/image_view/scad_split_view` → 按 Playwright 标签 `@markdown`/`@image`/`@scad-split` 过滤；`browser-smoke.spec.ts` 新增三条标签用例 + 显式点 `model.stl`；新增 fixtures `screenshot.png`（1×1 PNG）与 `examples/cube.scad`。12) 单测新增 19 条（markdown parser 8 + tab-kind 6 + ui-store 5）。Phase 6 经 codex review：gate PASS；[P2] markdown `javascript:` 链接 XSS → `isSafeLinkUrl` 白名单 http/https/mailto/tel + 相对路径，其他协议渲染为纯文本 + 加 `noopener`；[P2] 错误消息解包只看外层 → `describeFileReadError` 先读 `payload.message/code`（`ClientError::ProtocolError` 嵌套形态）再回退外层。验证：`cargo check --workspace` / `cargo test -p studio-common --tests` / typecheck / unit 21/21 / 全 S1a-S4 `web:smoke` 全绿 / 3 条扩展用例通过。grep 验收：`WorkspaceSession|PreviewState|DirectoryWatchLifecycle|AppServerClient` 零命中；`FileReadContents` 仅在 `file-read-decoder.ts` 解包。 | `.scad` 源码默认被 deny 意味着 ScadSplitViewer 目前总在源码面板展示 "denied"，若后续产品想让 web 端阅读 `.scad`，需要调整 `FileReadCapability` 默认（协议层决策，不是 UI 决策）。Markdown GFM（表格 / 任务 / 有序列表 / setext 标题 / 删除线）未支持。图片超 4096px 只 console.warn 未 UI toast。刷新持久化标签属协议级 session reclaim 范畴，不私造 localStorage。 |
| 7 | 已完成 | 3D 交互状态 + 参数 + 预设 + 导出 + 切片器 + 配置 + 日志 + 自动重渲染 + 平台限制项全部接回。`canvas/{camera-state,camera-controls,use-camera-controller}.ts` 维护相机状态机（7 个预设 + 纯函数式 orbit/zoom/pan），`workbench/canvas-toolbar.tsx` + `canvas-statusbar.tsx` 提供工具栏与状态栏，`renderer-controller.ts` 接受 cameraState 但桩层 no-op（真像素推到未来 phase）。`workbench/parameters-panel.tsx` 用手动 `name=value` 覆写（源码 regex / `ParsedParameters` 协议路径都未通，记录到 `docs/known_issues.md`）。`workbench/{preset-io,presets-panel}.tsx` 用 `<source>.presets.json` 经 FileRead/FileWriteText 持久化。`workbench/slicer-panel.tsx` 渲染 `SlicerListResponse` 且不尝试本地启动切片器。`workbench/export-panel.tsx` 驱动 `ExportRun`。`workbench/{log-panel,use-log-buffer}.tsx` 用 React ring buffer 收 TransportOpen/Closed / RequestFailed / Timeout / WatchResubscribed 事件；挂在 Inspector bottomSlot。`workbench/scad-workbench.tsx` 整合 scad-split-viewer + parameters + presets；watch event 命中活跃 scad tab 时触发 rerender（目录级，协议粒度限制记录）。`routes/settings.tsx` 真接入 `ConfigLoad` / `ConfigSave`，AppConfig 走组件级 useState 不入 store。新增 5 条 Playwright spec（canvas_interaction / parameters_presets / export_slicer / config_settings）与扩展 browser-watch-smoke 里的 `@scad-autorerender`，经共享 `_smoke-harness.ts` spawn host+vite。`scripts/run_smoke.ts` 识别全部 5 条新 case。新增 unit 11 条（camera-controls / preset-io）。`docs/web-platform-limits.md` §8-§12 追加 3D 交互 / 参数预设 / 导出 / 配置 / 日志的平台限制；`docs/known_issues.md` 追加 3 条协议缺口（`ParsedParameters` 未通 / `ExportRun.output_path` 绝对路径 / watch 粒度为目录级）——**未修改 server 源码**。Phase 7 经 codex review：gate PASS；[P2] ScadWorkbench 跨 tab overrides 泄漏 → `useEffect([path])` 清理；[P2] 预设文件首次不存在被当错误 → 新增 `isMissingPresetError` 识别 "No such file" / `not_found` code 双形态；[P2] `@config-settings` 写真实用户 `dirs::config_dir()` → harness 加 `hostEnv` 选项，config-settings spec 起一个 tmp HOME + XDG_CONFIG_HOME（保留 CARGO_HOME / RUSTUP_HOME 让 `cargo run` 仍可用）并 `afterAll` 清理；[P3] ExportPanel 切 tab 后 filename 不重置 → `useEffect([source, defaultFilename])` 重置。验证：`cargo check --workspace` / `cargo test -p studio-common --tests` / typecheck / unit 32/32 / 全 S1a-S4 `web:smoke` 全绿 / 5 条 Phase 7 case 均通过。grep：`AppConfig|SlicerConfig|ParsedParameters|PresetFile` 只在组件级 useState 命中，`app-server-transport` 在 studio-common 仍零命中。 | 真实 3D 渲染：camera state 已就绪但 wgpu async init 未解，推到独立 plan。协议扩展（`ParsedParameters` 通过 PreviewResponse 回传 / `ExportRun.output_path` 相对路径协议化 / `WatchChangedEvent` 文件级粒度）列入 `docs/known_issues.md`。字体离线自托管 / GFM Markdown / 响应式折叠仍推到后续 phase。 |
| 8 | 未开始 |  |  |

## Phase 0 Review 归档

- 独立 subagent review 发现 4 条 P0 + 5 条 P1 + 3 条 P2。
- P0#1 `client_dispatch_*` 签名冲突：已改为 `Result<RequestId, ClientError>`（bridge §1 / §5）。
- P0#2 首次握手触发时机未定义：已新增 `client_begin_handshake(handle, params)`（bridge §2）。
- P0#3 params / payload 字段缺席：已新增 §1.1 params → `app-server-protocol` 映射表；`WatchEventPayload` schema 写入 §7。
- P0#4 ownership §2 禁止项误伤 TS resolver table：已在 ownership §3 显式允许单一文件 `packages/studio-web/src/wasm-bridge/request-resolvers.ts`；bridge §10 同步放行。
- P1#5 `client_destroy` 语义补齐、`InvalidHandle` 纳入错误枚举。
- P1#6 `WatchEventPayload` 字段固定。
- P1#7 watch registry 归属显式写明在 `ManagedClient`。
- P1#8 `SCAD_STUDIO_WS_URL` URL vs 端口措辞统一。
- P1#9 S1c diff 文件清单明示。
- P2#10 `ManagedClient` 定名，删除备选。
- P2#11 `RequestId` 明确为 `app_server_protocol::RequestId` newtype，跨 JS 用 `bigint`。
- P2#12 本 Phase 0 结果存档已写入本文件（本块即是）。
