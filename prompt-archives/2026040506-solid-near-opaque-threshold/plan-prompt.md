## 背景

- `mini_itx_pc_003.scad` 的屏幕玻璃颜色为 `#1A1A2EF2`，alpha 约为 `0.95`。
- 已实现：
  - 透明/不透明分离绘制
  - 透明三角面按相机距离排序
- 用户反馈 `Solid` 模式仍有问题。
- 在真实模型里，这层玻璃几乎不透明，又紧贴浅色背板。继续按透明面混合，本身就容易产生不稳定的视觉结果；而 `Solid` 模式的优先目标应是稳定实体外观。

## 本轮目标

- 在 `Solid` 路径里把“近乎不透明”的三角面按不透明处理。
- 保持真正半透明面仍走透明 pass。
