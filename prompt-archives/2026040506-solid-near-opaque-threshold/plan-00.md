## Phase 1: 建立近乎不透明阈值测试

### 输入

- 当前 `triangle_index_partitions()` 对所有 `0 < alpha < 1` 的三角面都归为透明。

### 要保护的前序目标 / 边界

- 不回退透明分离和透明排序能力。

### 操作步骤

1. 在 `tests/mesh_tests.rs` 新增测试。
2. 约束 `alpha=0.95` 的三角面在 `Solid` 分区里应进入不透明索引。
3. 先运行测试，确认现状下失败。

### 验收标准

- 新测试先失败。

## Phase 2: 引入 Solid 近乎不透明阈值

### 输入

- Phase 1 的失败测试。

### 要保护的前序目标 / 边界

- 只影响 `Solid` 分区逻辑，不改 `X-Ray` 路径。

### 操作步骤

1. 在 `src/mesh.rs` 中为 `Solid` 分区增加 alpha 阈值。
2. 保持明显半透明面仍然进入透明 pass。

### 验收标准

- Phase 1 测试转绿。
- 现有透明排序测试仍通过。

## Phase 3: 回归验证

### 输入

- 已完成实现。

### 要保护的前序目标 / 边界

- 不破坏 `mesh`、`pipeline`、`three_mf` 测试。

### 操作步骤

1. 运行格式化。
2. 运行相关测试。
3. 记录结果。

### 验收标准

- 相关测试通过。
