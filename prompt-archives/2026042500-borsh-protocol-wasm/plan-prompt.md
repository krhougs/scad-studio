# 原始 Prompt

## 2026-04-25

我们现在进行一个架构上的重构，你需要仔细深度研究怎么做，然后出plan之后我们继续讨论。

1. studio-web和后端交互用的序列化/反序列化方式效率太低，需要换成二进制
2. 我要求使用 `borsh` 作为rust端的序列化方案
3. 新增protocol wasm包和对应的ts包用来把数据结构暴露给js侧
4. 本次重构需保证现有功能完全工作并迁移，不得留有残留的JSON
5. 本次重构不涉及studio-app内部基于内存的通信

请仔细研究相关技术栈，给出迁移方案，问我东西尽量一次性问清楚

## 2026-04-25 补充确认

1. 我们现在只涉及 wire protocol，不涉及各种配置和预设文件。
2. 我们项目已经定名 budn，npm scope 得是这个，之前有的包的 scope 也得改。
3. 禁止 TS 手工构造完整 protocol envelope，序列化 / 反序列化完全交给 wasm。
4. 完全迁移，不要留任何债。
5. 对 `ExportRun.output_path` 问题不理解，需要解释。

## 2026-04-25 路径策略补充

我们作为一个跨平台的应用，要严格限制文件名 / 路径跨平台兼容性。研究限制方式，写文档，并补充进已有 Borsh protocol wasm 迁移计划。

## 2026-04-25 路径策略修正

ASCII 太严格，至少支持 CJK 和常见 emoji。过滤掉常见会造成问题的符号。允许相对路径的链接。

## 2026-04-25 JS 项目兼容性补充

常见 JS 项目里的文件名都得兼容。

## 2026-04-25 plan 结构修正

plan 有问题：

1. 已经确认方案后应该删除噪音。
2. portable path 应该是新 wire 协议中的一个重要限制条件，而不是和 Borsh 协议同级别的东西。

## 2026-04-25 浮点数提示修正与独立 review

1. 不能删除浮点数相关的重要提示。
2. 修正后开启独立 subagent review plan，根据 review 结果修正 plan，必要时直接重写 plan。
